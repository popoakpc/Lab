-----------------------------------------------
Initialize an empty git repository within this directory.

Add a remote named "origin", pointing to the url of the 
remote repository you created in Lab1.

Add a file named myid.sh which print your student id, 
so we can recognize you.

Stage and commit you changes with the commit message "inital commit", however, 
becareful not to stage readme.txt as it should not be a part of this project.

Push local changes to remote.